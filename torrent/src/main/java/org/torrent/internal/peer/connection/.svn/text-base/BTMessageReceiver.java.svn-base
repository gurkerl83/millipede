package org.torrent.internal.peer.connection;

import org.torrent.internal.protocol.message.BittorrentMessage;

/**
 * 
 * @author dante
 * 
 */
public interface BTMessageReceiver {
	/**
	 * Invoked if a message was received.
	 * 
	 * @param message
	 *            the received message
	 */
	void received(BittorrentMessage message);
}
